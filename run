#!/bin/sh
# Release entrypoint. The firefly supervisor launches /srv/camptool/current/run
# with the release dir as cwd; cd here too so startup migrations resolve
# db/migrations relative to this release. Runs under Bun (the app uses bun:sqlite).
cd "$(dirname "$0")" || exit 1
exec bun server.ts
